import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    zIndex: 1,
    height: '100%',
    width: '100%',
    backgroundColor: '#FFF',
  },
});
